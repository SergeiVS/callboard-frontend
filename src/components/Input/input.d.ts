import { InputProps } from "./types";
declare const Input: ({ value, onChange, error, placeholder, label, name, type, disabled, multiline, rows }: InputProps) => import("react/jsx-runtime").JSX.Element;
export default Input;
